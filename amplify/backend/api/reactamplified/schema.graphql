type Todo @model
@auth(rules: [
    { allow: owner }
  ]){
  id: ID!
  name: String!
  description: String
  status: Boolean
  priority: String
  owner : String
  editors : [String]
}

type Query {
  upload(msg: String): String @function(name: "fileupload-${env}")
}
