type Todo @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
  description: String
  status: Boolean
  priority: String
}

type Query {
  upload(msg: String): String @function(name: "fileupload-${env}")
}
